<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:AvaloniaPrism.ViewModels"
             xmlns:mvvm="http://prismlibrary.com/"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="AvaloniaPrism.Views.ServicesView"
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             x:DataType="viewModels:ServicesViewModel">
    <StackPanel>
        <TextBlock Text="Welcome to ServicesView!" />
        <TextBlock Text="{Binding DirectSubtitle}" />
        <TextBlock Text="{Binding FuncSubtitle}" />
        <TextBlock Text="{Binding LazySubtitle}" />
        <TextBlock Text="{Binding ManyFooServicesSubtitle}" />
        <TextBlock Text="{Binding KeyedFirstSubtitle}" />
        <TextBlock Text="{Binding KeyedSecondSubtitle}" />
    </StackPanel>
</UserControl>
